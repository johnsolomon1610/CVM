<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cvm">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CVM">
        <activity android:name=".User.User_Homepage"></activity>
        <activity android:name=".Authentication.Verify_otp" />
        <activity android:name=".Authentication.LoginActivity" />
        <activity android:name=".User.User_registration_page" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Admin.Admin_registration_page">

        </activity>
    </application>

</manifest>